<!--
@file: Diary.md
@description: Дневник наблюдений, решений и проблем по проекту
@dependencies: Project.md, Tasktracker.md, qa.md, changelog.md
@created: 2026-03-23
-->

# Diary

## [2026-03-23] Инициализация архитектурного контура

### Наблюдения
- Проект ориентирован на быстрый MVP, где главный KPI — запись за 10 секунд.
- Критичны простота UI, отсутствие когнитивной нагрузки и минимальное число шагов.
- Основной риск — коллизии слотов и неконсистентность при отменах/напоминаниях.

### Решения
- Зафиксирована слоистая архитектура (adapter -> application -> domain -> infrastructure).
- Для MVP выбран Supabase (PostgreSQL) как целевая БД.
- Отложенные уведомления решено реализовать через APScheduler с восстановлением задач при старте.
- В документации закреплены принципы SOLID/KISS/DRY и критерии готовности.

### Проблемы
- Не определен окончательный формат хранения рабочего расписания (в БД или в конфиге).
- Не уточнены требования к часовому поясу и режиму летнего времени.
- Не зафиксирован окончательный перечень администраторов (один или несколько user_id).

## [2026-03-23] Подготовка двух технических заданий

### Наблюдения
- Появилась необходимость формально разделить MVP-объем и расширенный объем.
- Для расширенного ТЗ принята гипотеза: по всем пунктам `qa.md` ответы положительные.

### Решения
- Создано отдельное ТЗ для MVP: `docs/TZ_MVP.md`.
- Создано отдельное расширенное ТЗ: `docs/TZ_EXTENDED_ALL_POSITIVE.md`.
- В расширенном варианте зафиксированы дополнительные требования по админам, расписанию, уведомлениям, политике данных и эксплуатации.

### Проблемы
- Требуются окончательные подтверждения параметров (например, горизонт записи, среда развертывания), чтобы перейти от гипотез к утвержденным требованиям.

## [2026-03-23] Старт реализации MVP-каркаса

### Наблюдения
- Пользователь подтвердил переход к реализации согласно `docs/TZ_MVP.md`.
- На текущем шаге приоритет — создать фактическую структуру кода и базовые модули без перегрузки логикой.

### Решения
- Сначала актуализируется документация, затем создается каркас `src/` и `tests/`.
- Базовая реализация выполняется на Python + aiogram 3.x с модульной структурой слоев.

### Проблемы
- Полная бизнес-логика записи и интеграция БД будут реализованы следующими шагами; на этом этапе формируется foundation.

## [2026-03-23] Реализация MVP foundation: структура и базовые модули

### Наблюдения
- Создана фактическая структура `src/` и `tests/` согласно `TZ_MVP.md`.
- Добавлены базовые хендлеры, сервисы, конфиг, модели и стартовые тесты.

### Решения
- Зафиксирован единый вход приложения в `src/main.py`.
- Базовая архитектура отражает разделение на bot/app/domain/infra.
- Добавлены `requirements.txt` и `.env.example` для ускорения последующей сборки.

### Проблемы
- Временная демо-логика в хендлерах и сервисах требует замены на реальную БД- и FSM-логику.
- Не настроены линтеры и pre-commit hooks (задача остается открытой).

## [2026-03-23] Реализация реальной бизнес-логики записи (Supabase + FSM)

### Наблюдения
- Для MVP выбран Supabase (PostgreSQL), а не SQLite.
- Сценарий записи удобно реализовывать через FSM: регистрация (контакт+имя) и затем booking (дата/время/подтверждение).

### Решения
- Добавлены FSM states `RegistrationStates` и `BookingStates`.
- `/start` реализован как регистрационный сценарий с явным согласием на контакт.
- Сценарий `📅 Записаться` переведен на inline-клавиатуры с выбором даты и слота.
- Добавлена бизнес-логика подтверждения и отмены через `BookingService` и Supabase-репозитории.
- `/today`, `/tomorrow`, `/all` реализованы через чтение из `appointments` и подтягивание `name/phone` из `users`.

### Проблемы
- Возможны гонки при одновременной записи в один слот: для полной защиты нужен уникальный constraint в БД (например, на (date,time_slot) для `status='confirmed'`) или RPC-операция.

## [2026-03-23] Публикация кода в GitHub и подготовка Supabase схемы

### Наблюдения
- Публикация текущего состояния проекта в удаленный Git-репозиторий выполнена успешно.
- Перед следующими доработками необходима корректная схема Supabase для `users` и `appointments`.

### Решения
- Подготовлена SQL-схема и индексы для Supabase с частичным уникальным индексом слота `status='confirmed'`.

### Проблемы
- Бот пока не обрабатывает ошибку уникального constraint как “слот занят” (нужно будет добавить catch/сообщения после создания индекса и проверки реального поведения).

## [2026-03-23] Обработка unique_violation при конкурентной записи

### Наблюдения
- После создания уникального индекса `appointments_confirmed_slot_unique` при конкурентной записи возможна ошибка вставки вместо “предварительной проверки”.

### Решения
- В `src/infra/db/repositories/appointments_repository.py` добавлен перехват ошибок вставки.
- Ошибка уникальности (unique_violation/23505/duplicate key/имя индекса) преобразуется в `SlotUnavailableError`, который в UI соответствует состоянию “слот уже занят”.

### Проблемы
- Для полной уверенности нужны интеграционные тесты/ручной гоняющий сценарий.

## [2026-03-23] Исправление insert().select в supabase-py

### Наблюдения
- При нажатии кнопки “✅ Подтвердить” бот падал с `AttributeError: 'SyncQueryRequestBuilder' object has no attribute 'select'`.
- Причина: supabase-py 2.x не поддерживает цепочку `.insert(...).select(...)`.

### Решения
- В `src/infra/db/repositories/appointments_repository.py` удалена `.select()` после `insert`.
- После вставки запись того же слота выбирается отдельным запросом `select(...)`.
- Добавлена “страховка” в UI-обработчик `confirm_or_back`, чтобы не было тишины при внутренних ошибках.

### Проблемы
- Нужна финальная проверка end-to-end сценария в Telegram после перезапуска polling.

## [2026-03-23] Настройка MVP: шаг слота 60 мин и кнопки 3 в ряд

### Наблюдения
- Пользователь подтвердил фиксацию длительности слота 60 минут.
- Также требуется улучшить UX: вывод picker-кнопок (даты/времени) по 3 в ряд.

### Решения
- В `src/app/services/schedule_service.py` изменен `DEFAULT_STEP_MINUTES` на `60`.
- В `src/bot/keyboards/booking.py` перестроены inline-клавиатуры:
  - `date_picker_keyboard`: по 3 даты в строке.
  - `time_picker_keyboard`: по 3 слота в строке.
- В `docs/TZ_MVP.md` обновлено описание длительности слота.

### Проблемы
- Для корректного отражения изменений в запущенном боте требуется перезапуск polling.

## [2026-03-23] Реализация must-have напоминаний (24ч и 2ч)

### Наблюдения
- Была необходимость сделать отправку напоминаний устойчивой к перезапускам бота.
- Supabase unique-индикатор слота уже добавлен, для напоминаний требуется идемпотентность.

### Решения
- Добавлен `reminder_jobs` в Supabase (ваш SQL).
- Создан репозиторий `ReminderJobsRepository` и обновлен `ReminderService`:
  - `schedule_reminders()` планирует 24ч и 2ч после подтверждения записи
  - `send_due_reminders()` раз в минуту отправляет due/unsent
- В `src/main.py` добавлен периодический воркер через `AsyncIOScheduler` (APScheduler).

### Проблемы
- Точность времени зависит от трактовки timezone (MVP использует UTC).

## [2026-03-23] Доработка напоминаний: отмена и очистка jobs

### Наблюдения
- Напоминания должны переставать отправляться после отмены записи.
- Также важно не крутить бесконечно jobs для отмененных/удаленных appointments.

### Решения
- Добавлена логика деактивации unsent reminder jobs при отмене записи.
- В воркере отправки jobs для неактивных appointments отмечаются как `sent`.

### Проблемы
- Для бизнес-точности времени понадобится выбор и фиксация timezone барбера (post-MVP/следующий шаг).

## [2026-03-24] Исправление падения scheduler-воркера напоминаний

### Наблюдения
- В логах APScheduler фиксировалась ошибка `RuntimeError: no running event loop`.
- Из-за этого воркер `send_due_reminders()` фактически не исполнялся.

### Решения
- В `src/main.py` job переведен на `async def` и прямой `await reminder_service.send_due_reminders(bot)`.
- Убран вызов `asyncio.create_task(...)` из sync-функции scheduler.

### Проблемы
- Нужна повторная ручная проверка доставки due-напоминаний после перезапуска бота.

## [2026-03-24] Добавлен test mode для напоминаний через .env

### Наблюдения
- Для ручной проверки неудобно ждать реальные 24ч/2ч.

### Решения
- Добавлены настройки в `Settings`:
  - `REMINDER_24H_OFFSET_MINUTES` (по умолчанию 1440)
  - `REMINDER_2H_OFFSET_MINUTES` (по умолчанию 120)
- `ReminderService` теперь использует offsets из env.
- В `.env.example` добавлены новые параметры.
- Добавлен unit-тест с переопределением offsets через env.

### Проблемы
- После изменения `.env` нужно перезапустить бота, иначе старые значения останутся в памяти.

## [2026-03-24] Перевод напоминаний на timezone UTC+3

### Наблюдения
- Для проекта требуется расчет напоминаний по локальному времени барбера (UTC+3), а не по UTC.

### Решения
- Добавлена конфигурация `APP_TIMEZONE` (по умолчанию `Europe/Minsk`).
- В `ReminderService` расчет времени записи и offsets переведен на локальный timezone с последующей конвертацией в UTC при сохранении в БД.

### Проблемы
- При изменении `APP_TIMEZONE` в `.env` требуется перезапуск бота.

## [2026-03-24] APP_TIMEZONE: поддержка числового формата

### Наблюдения
- Пользователь хочет указывать timezone числом в `.env` вместо IANA-строки.

### Решения
- В `ReminderService` добавлен парсер timezone:
  - поддерживаются `3`, `+3`, `UTC+3`, `GMT+3`
  - также продолжает работать IANA формат (`Europe/Minsk`)
- В `.env.example` добавлены примеры допустимых форматов.
- Добавлен unit-тест для числового `APP_TIMEZONE`.

### Проблемы
- После смены значения в `.env` необходим перезапуск бота.

## [2026-03-24] Косметика UX: чистый чат в booking-сценарии

### Наблюдения
- Пользовательский поток записи создавал слишком много технических сообщений.

### Решения
- В `booking.py` вместо цепочки `answer(...)` на каждом шаге внедрено переиспользование одного сообщения:
  - выбор даты → edit того же сообщения
  - выбор времени → edit того же сообщения
  - подтверждение/ошибки → edit того же сообщения
- После успешного подтверждения старое prompt-сообщение удаляется, а остается только итоговое подтверждение.

### Проблемы
- Для проверки требуется ручной прогон в Telegram (поток callback/edit/delete зависит от клиента и прав бота).

## [2026-03-24] Персонализация сообщений по имени пользователя

### Наблюдения
- Требуется обращение к пользователю по имени в ключевых ответах бота.

### Решения
- Добавлено обращение по имени в:
  - приветствии после `/start` для зарегистрированного пользователя;
  - сообщении после успешной регистрации;
  - блоке `📋 Моя запись`;
  - блоке `❌ Отменить запись`;
  - подтверждении успешной записи.

### Проблемы
- При отсутствии имени применяется fallback `Клиент`.

## [2026-03-25] Настройка рабочего графика барбера

### Наблюдения
- Для корректной генерации слотов нужно хранить рабочие дни/часы в БД и использовать их в `ScheduleService`.
- Требовалось расширить админский контур командами настройки без ручного ввода сложных интерфейсов.

### Решения
- Добавлен репозиторий `WorkScheduleRepository`, который читает актуальную запись из `public.work_schedule` и позволяет сохранять новую (MVP: `delete + insert` одной “актуальной” записи).
- `ScheduleService` переведен на async-загрузку графика: `next_working_dates()` и `get_candidate_slots_for_date()` теперь опираются на сохраненный график.
- Расширен `src/bot/handlers/admin.py` командами:
  - `/schedule` — показать текущий график
  - `/set_schedule 1,2,3,4,5 10:00 18:00` — сохранить график (1=Пн ... 7=Вс)
- Обновлен booking-флоу: даты берутся через `schedule_service.next_working_dates()`, а доступные слоты — через async `BookingService.list_available_time_slots()`.

### Проблемы
- Для полноценной гарантии “точности” по времени следует синхронизировать формат времени в Supabase (строки `HH:MM:SS`) с парсером в репозитории.

## [2026-03-25] Админ-панель по коду /admin

### Наблюдения
- Требовалось отделить доступ с кнопками только для админа от текущих `/today`/`/all`.
- Нужно было обеспечить интерактивное меню без ручного ввода сложных команд.

### Решения
- Добавлен параметр `ADMIN_PANEL_ACCESS_CODE` в конфигурацию.
- Реализован вход по `/admin` с запросом кода и FSM-стейтом.
- После входа показываются кнопки:
  - “Расписание барбера” (текущая запись `work_schedule` + слоты на сегодня)
  - “Приёмы на сегодня” (чтение `appointments` и подгрузка `users`).

## [2026-03-26] Уведомление админа о новых записях

### Наблюдения
- Нужно уведомлять админа сразу после подтверждения клиентом записи.
- Требовалось определять админов на основе роли в Supabase, без дополнительной привязки к чатам.

### Решения
- Добавлено поле `role` в `UserModel` и расширен `UsersRepository` для выбора админов (`role='admin'`).
- В `src/bot/handlers/booking.py` после успешного `create_appointment` отправляется сообщение всем админам.

## [2026-03-26] Правка кнопок админ-панели

### Наблюдения
- Требуется упростить админ-меню и оставить только быстрый просмотр записей по дням.

### Решения
- В `src/bot/handlers/admin.py` заменены кнопки админ-панели на:
  - “Записи на сегодня”
  - “Записи на завтра”.
- Добавлен callback-обработчик вывода “Записи на завтра”.
